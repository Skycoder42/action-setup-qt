name: Test the action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: Skycoder42/action-setup-qt@master
      with:
        version: 5.13.1
        platform: gcc_64
        packages: qtnetworkauth,qtwebengine
        install-args: "--addRepository https://install.skycoder42.de/qtmodules/linux_x64 --verbose"
